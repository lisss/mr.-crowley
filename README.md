# Crawley

Minimal notes
- Start UI: `python web.py` then open http://localhost:5002
- CLI crawl: `python crawler.py https://example.com/ --level 1`
- Docker: `docker-compose up -d` then UI at http://localhost:5002

Deploy: see `DEPLOY.md`
